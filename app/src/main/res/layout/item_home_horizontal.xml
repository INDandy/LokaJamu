<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="140dp"
    android:layout_height="200dp"
    android:layout_marginLeft="8dp"
    android:layout_marginRight="8dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_home_horizontal"
        android:layout_width="match_parent"
        android:layout_height="175dp"
        app:cardCornerRadius="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_home_horizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="gambar artikel terbaru"
            android:scaleType="centerCrop"
            app:srcCompat="@drawable/lokajamulogo" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_home_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginTop="6dp"
        android:text="Name"
        android:textColor="@color/hijau"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/card_home_horizontal" />
</androidx.constraintlayout.widget.ConstraintLayout>